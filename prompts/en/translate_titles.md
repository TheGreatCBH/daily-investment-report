Normalize the following news headlines to clear, tight English (if the headline is already English, return it cleaned up; if it is Chinese, translate). Return JSON:

```json
{{
  "translations": [
    {{"original": "source headline", "translated": "English headline"}}
  ]
}}
```

{titles_list}
